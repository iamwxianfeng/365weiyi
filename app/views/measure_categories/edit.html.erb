<h1>Editing measure_category</h1>

<% form_for(@measure_category) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @measure_category %> |
<%= link_to 'Back', measure_categories_path %>
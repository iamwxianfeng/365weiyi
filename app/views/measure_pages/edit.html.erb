<h1>Editing measure_page</h1>

<% form_for(@measure_page) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @measure_page %> |
<%= link_to 'Back', measure_pages_path %>